var chai = require('chai');
var expect = chai.expect;

var lib = require('../lib/exercicio.js');

describe('Iniciando teste', function(){
	it('Teste de exemplo que não testa nada', function(){
		expect(0).to.be.equal(0);
	});
});
